class Solution{   
public:
    int getPairsCount(int arr[], int n, int k) {
       //unordered_map <int,int> freq;
       int count=0;
        for(int i=0;i<n;i++){
           // count+=freq[k-arr[i]];  arr[i]+arr[j]=K//intra 
           // freq[arr[i]]++;
        }
        return count;
    }
}